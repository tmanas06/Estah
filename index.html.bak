<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Estah Society — Run for a Child's Future</title>
    <meta name="description"
        content="Estah Society empowers communities through education, entrepreneurship, and sustainability under the WE HEAL THE EARTH framework.">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;0,700;1,400;1,500;1,600&family=DM+Sans:wght@300;400;500;600;700&display=swap"
        rel="stylesheet">
    <style>
        *,
        *::before,
        *::after {
            margin: 0;
            padding: 0;
            box-sizing: border-box
        }

        :root {
            --bg: #0A0A08;
            --surface1: #141410;
            --surface2: #1C1C18;
            --gold: #C9A84C;
            --gold-light: #F0D080;
            --green: #4E8C6F;
            --text: #EDE8DE;
            --muted: #7A7260;
            --red: #C94C4C;
            --font-display: 'Cormorant Garamond', serif;
            --font-body: 'DM Sans', sans-serif;
            --nav-h: 64px;
        }

        html {
            scroll-behavior: smooth
        }

        body {
            font-family: var(--font-body);
            background: var(--bg);
            color: var(--text);
            line-height: 1.6;
            overflow-x: hidden;
            min-height: 100vh
        }

        h1,
        h2,
        h3,
        h4,
        h5 {
            font-family: var(--font-display);
            font-weight: 600;
            line-height: 1.2
        }

        a {
            color: inherit;
            text-decoration: none
        }

        button {
            font-family: var(--font-body);
            cursor: pointer;
            border: none;
            background: none;
            color: inherit
        }

        img {
            max-width: 100%;
            display: block
        }

        .grain {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 9999;
            opacity: .03
        }

        .grain svg {
            width: 100%;
            height: 100%
        }

        /* NAV */
        .nav {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: var(--nav-h);
            z-index: 1000;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 40px;
            background: rgba(10, 10, 8, .88);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(201, 168, 76, .12)
        }

        .nav-logo {
            font-family: var(--font-display);
            font-size: 1.35rem;
            font-weight: 600;
            color: var(--gold);
            letter-spacing: .5px;
            white-space: nowrap
        }

        .nav-links {
            display: flex;
            gap: 8px;
            align-items: center
        }

        .nav-link {
            position: relative;
            padding: 8px 14px;
            font-size: .82rem;
            font-weight: 500;
            letter-spacing: .04em;
            color: var(--muted);
            transition: color .25s;
            display: flex;
            align-items: center;
            gap: 6px
        }

        .nav-link::after {
            content: '';
            position: absolute;
            bottom: 2px;
            left: 14px;
            right: 14px;
            height: 1.5px;
            background: var(--gold);
            transform: scaleX(0);
            transition: transform .3s cubic-bezier(.25, .46, .45, .94);
            transform-origin: left
        }

        .nav-link:hover {
            color: var(--text)
        }

        .nav-link:hover::after {
            transform: scaleX(1)
        }

        .nav-link.active {
            color: var(--gold)
        }

        .nav-link.active::after {
            transform: scaleX(1)
        }

        .nav-link.active .dot {
            display: block
        }

        .dot {
            display: none;
            width: 4px;
            height: 4px;
            border-radius: 50%;
            background: var(--gold);
            position: absolute;
            bottom: -4px;
            left: 50%;
            transform: translateX(-50%)
        }

        .soon-badge {
            font-size: .6rem;
            padding: 2px 6px;
            border-radius: 8px;
            background: rgba(122, 114, 96, .25);
            color: var(--muted);
            font-weight: 600;
            letter-spacing: .06em;
            line-height: 1
        }

        .hamburger {
            display: none;
            flex-direction: column;
            gap: 5px;
            cursor: pointer;
            padding: 8px;
            z-index: 1001
        }

        .hamburger span {
            display: block;
            width: 22px;
            height: 2px;
            background: var(--text);
            border-radius: 2px;
            transition: all .3s
        }

        .hamburger.open span:nth-child(1) {
            transform: rotate(45deg) translate(5px, 5px)
        }

        .hamburger.open span:nth-child(2) {
            opacity: 0
        }

        .hamburger.open span:nth-child(3) {
            transform: rotate(-45deg) translate(5px, -5px)
        }

        .mobile-menu {
            display: none;
            position: fixed;
            top: 0;
            right: -100%;
            width: 280px;
            height: 100vh;
            background: var(--surface1);
            z-index: 999;
            padding: 80px 32px 40px;
            transition: right .35s cubic-bezier(.25, .46, .45, .94);
            border-left: 1px solid rgba(201, 168, 76, .12);
            flex-direction: column;
            gap: 4px
        }

        .mobile-menu.open {
            right: 0
        }

        .mobile-menu .nav-link {
            padding: 14px 0;
            font-size: 1rem;
            border-bottom: 1px solid rgba(255, 255, 255, .04)
        }

        .mobile-menu .nav-link::after {
            display: none
        }

        .mobile-overlay {
            display: none;
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, .6);
            z-index: 998
        }

        .mobile-overlay.open {
            display: block
        }

        /* PAGES */
        .page {
            display: none;
            padding-top: var(--nav-h);
            min-height: 100vh;
            animation: pageFadeIn .4s ease
        }

        .page.active {
            display: block
        }

        @keyframes pageFadeIn {
            from {
                opacity: 0;
                transform: translateY(12px)
            }

            to {
                opacity: 1;
                transform: translateY(0)
            }
        }

        /* HERO */
        .events-hero {
            min-height: 420px;
            display: flex;
            align-items: center;
            padding: 80px 60px 60px;
            position: relative;
            overflow: hidden
        }

        .events-hero::before {
            content: '';
            position: absolute;
            top: -100px;
            right: -100px;
            width: 600px;
            height: 600px;
            background: radial-gradient(circle, rgba(201, 168, 76, .08) 0%, transparent 70%);
            pointer-events: none
        }

        .hero-content {
            position: relative;
            z-index: 1;
            max-width: 700px
        }

        .eyebrow {
            font-family: var(--font-body);
            font-size: .75rem;
            font-weight: 600;
            letter-spacing: .18em;
            text-transform: uppercase;
            color: var(--gold);
            margin-bottom: 20px;
            animation: fadeUp .6s ease both
        }

        .hero-title {
            font-size: clamp(2.4rem, 5vw, 3.8rem);
            font-weight: 300;
            margin-bottom: 20px;
            animation: fadeUp .6s ease .15s both
        }

        .hero-title em {
            font-style: italic;
            color: var(--gold);
            font-weight: 400
        }

        .hero-sub {
            font-size: 1.05rem;
            color: var(--muted);
            max-width: 560px;
            line-height: 1.65;
            animation: fadeUp .6s ease .3s both
        }

        @keyframes fadeUp {
            from {
                opacity: 0;
                transform: translateY(20px)
            }

            to {
                opacity: 1;
                transform: translateY(0)
            }
        }

        /* FILTER BAR */
        .filter-bar {
            position: sticky;
            top: var(--nav-h);
            z-index: 100;
            padding: 16px 60px;
            background: rgba(10, 10, 8, .92);
            backdrop-filter: blur(12px);
            border-bottom: 1px solid rgba(255, 255, 255, .04);
            display: flex;
            gap: 10px;
            overflow-x: auto;
            -webkit-overflow-scrolling: touch
        }

        .filter-bar::-webkit-scrollbar {
            display: none
        }

        .filter-pill {
            padding: 8px 20px;
            border-radius: 100px;
            font-size: .8rem;
            font-weight: 500;
            letter-spacing: .03em;
            border: 1px solid transparent;
            transition: all .25s;
            white-space: nowrap;
            cursor: pointer
        }

        .filter-pill.active {
            background: var(--gold);
            color: var(--bg);
            font-weight: 600
        }

        .filter-pill:not(.active) {
            background: var(--surface2);
            color: var(--muted);
            border-color: rgba(255, 255, 255, .06)
        }

        .filter-pill:not(.active):hover {
            border-color: var(--gold);
            color: var(--text)
        }

        /* EVENTS GRID */
        .events-section {
            padding: 40px 60px 80px
        }

        .section-label {
            font-size: .72rem;
            font-weight: 600;
            letter-spacing: .16em;
            text-transform: uppercase;
            color: var(--muted);
            margin-bottom: 28px
        }

        .events-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(360px, 1fr));
            gap: 28px
        }

        /* EVENT CARD */
        .event-card {
            background: var(--surface1);
            border-radius: 18px;
            border: 1px solid rgba(255, 255, 255, .06);
            overflow: hidden;
            cursor: pointer;
            transition: all .35s cubic-bezier(.25, .46, .45, .94)
        }

        .event-card:hover {
            transform: translateY(-6px);
            border-color: rgba(201, 168, 76, .35);
            box-shadow: 0 24px 64px rgba(0, 0, 0, .5), 0 0 0 1px rgba(201, 168, 76, .2)
        }

        .card-img-wrap {
            position: relative;
            height: 240px;
            overflow: hidden
        }

        .card-img-wrap img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform .5s
        }

        .event-card:hover .card-img-wrap img {
            transform: scale(1.04)
        }

        .card-img-fallback {
            width: 100%;
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #1A1A0D, #2A2000);
            font-size: 3rem
        }

        .card-img-overlay {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            height: 50%;
            background: linear-gradient(to top, rgba(20, 20, 8, .8) 0%, transparent 100%);
            pointer-events: none
        }

        .card-date-badge {
            position: absolute;
            top: 14px;
            left: 14px;
            background: rgba(201, 168, 76, .9);
            color: var(--bg);
            padding: 6px 14px;
            border-radius: 100px;
            font-family: var(--font-display);
            font-weight: 700;
            font-size: .85rem;
            letter-spacing: .02em
        }

        .card-cat-badge {
            position: absolute;
            top: 14px;
            right: 14px;
            background: rgba(78, 140, 111, .35);
            color: #8fd4b0;
            padding: 4px 12px;
            border-radius: 100px;
            font-size: .68rem;
            font-weight: 600;
            letter-spacing: .06em;
            text-transform: uppercase;
            backdrop-filter: blur(8px)
        }

        .card-body {
            padding: 24px
        }

        .card-title {
            font-family: var(--font-display);
            font-size: 1.4rem;
            font-weight: 600;
            margin-bottom: 12px;
            color: var(--text)
        }

        .card-org {
            display: flex;
            align-items: center;
            gap: 8px;
            margin-bottom: 14px
        }

        .card-org-avatar {
            width: 24px;
            height: 24px;
            border-radius: 50%;
            background: var(--gold);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: .65rem;
            font-weight: 700;
            color: var(--bg)
        }

        .card-org span {
            font-size: .82rem;
            color: var(--muted)
        }

        .card-meta {
            display: flex;
            flex-direction: column;
            gap: 8px;
            margin-bottom: 14px
        }

        .card-meta-row {
            display: flex;
            align-items: flex-start;
            gap: 8px;
            font-size: .82rem;
            color: var(--muted)
        }

        .card-meta-row svg {
            flex-shrink: 0;
            margin-top: 2px
        }

        .card-desc {
            font-size: .85rem;
            color: var(--muted);
            line-height: 1.55;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
            margin-bottom: 16px
        }

        .card-divider {
            height: 1px;
            background: rgba(255, 255, 255, .06);
            margin-bottom: 16px
        }

        .card-footer {
            display: flex;
            align-items: center;
            justify-content: space-between
        }

        .sold-out-badge {
            background: var(--red);
            color: #fff;
            font-size: .68rem;
            font-weight: 700;
            letter-spacing: .1em;
            text-transform: uppercase;
            padding: 5px 12px;
            border-radius: 6px
        }

        .view-details {
            color: var(--gold);
            font-size: .85rem;
            font-weight: 500;
            display: flex;
            align-items: center;
            gap: 6px;
            transition: gap .25s
        }

        .event-card:hover .view-details {
            gap: 10px
        }

        /* MODAL */
        .modal-overlay {
            display: none;
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, .88);
            backdrop-filter: blur(8px);
            z-index: 2000;
            justify-content: center;
            align-items: flex-start;
            padding: 40px 20px;
            overflow-y: auto
        }

        .modal-overlay.open {
            display: flex
        }

        .modal {
            max-width: 820px;
            width: 100%;
            background: var(--surface1);
            border-radius: 24px;
            border: 1px solid rgba(201, 168, 76, .18);
            overflow: hidden;
            animation: modalIn .4s ease both;
            position: relative;
            margin: auto
        }

        @keyframes modalIn {
            from {
                opacity: 0;
                transform: translateY(40px)
            }

            to {
                opacity: 1;
                transform: translateY(0)
            }
        }

        .modal-hero {
            position: relative;
            height: 340px;
            overflow: hidden
        }

        .modal-hero img {
            width: 100%;
            height: 100%;
            object-fit: cover
        }

        .modal-hero-overlay {
            position: absolute;
            inset: 0;
            background: linear-gradient(to top, var(--surface1) 0%, transparent 60%)
        }

        .modal-close {
            position: absolute;
            top: 16px;
            right: 16px;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: rgba(20, 20, 16, .7);
            backdrop-filter: blur(8px);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.1rem;
            color: var(--text);
            transition: all .25s;
            z-index: 1;
            border: 1px solid rgba(255, 255, 255, .1)
        }

        .modal-close:hover {
            background: var(--red);
            color: #fff;
            border-color: var(--red)
        }

        .modal-title {
            position: absolute;
            bottom: 24px;
            left: 36px;
            right: 36px;
            font-family: var(--font-display);
            font-size: clamp(1.8rem, 4vw, 2.6rem);
            font-weight: 600;
            color: var(--text);
            z-index: 1
        }

        .modal-body {
            padding: 36px 44px 48px
        }

        .modal-chips {
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
            margin-bottom: 32px
        }

        .modal-chip {
            display: flex;
            align-items: center;
            gap: 10px;
            background: var(--surface2);
            border: 1px solid rgba(255, 255, 255, .08);
            border-radius: 12px;
            padding: 12px 18px;
            font-size: .85rem;
            color: var(--text)
        }

        .modal-chip .chip-icon {
            font-size: 1.1rem
        }

        .modal-chip .chip-label {
            color: var(--muted);
            font-size: .78rem
        }

        .modal-chip .chip-value {
            font-size: .85rem
        }

        .modal-section {
            margin-bottom: 36px
        }

        .modal-section-title {
            font-family: var(--font-display);
            font-size: 1.5rem;
            color: var(--gold);
            margin-bottom: 16px;
            font-weight: 600
        }

        .modal-section p {
            color: var(--muted);
            line-height: 1.75;
            font-size: .92rem;
            margin-bottom: 12px
        }

        .mission-box {
            border-left: 4px solid var(--gold);
            background: rgba(201, 168, 76, .06);
            border-radius: 0 12px 12px 0;
            padding: 20px 24px;
            margin-top: 20px
        }

        .mission-box p {
            font-family: var(--font-display);
            font-style: italic;
            font-size: 1.05rem;
            color: var(--gold-light);
            line-height: 1.6;
            margin: 0
        }

        .impact-grid {
            display: flex;
            flex-wrap: wrap;
            gap: 14px
        }

        .impact-chip {
            background: var(--surface2);
            border: 1px solid rgba(255, 255, 255, .06);
            border-radius: 14px;
            padding: 18px 22px;
            flex: 1;
            min-width: 140px;
            text-align: center
        }

        .impact-num {
            font-family: var(--font-display);
            font-size: 1.8rem;
            font-weight: 700;
            color: var(--gold);
            margin-bottom: 4px
        }

        .impact-label {
            font-size: .72rem;
            color: var(--muted);
            letter-spacing: .04em
        }

        .awards-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 12px
        }

        .award-card {
            background: var(--surface2);
            border: 1px solid rgba(255, 255, 255, .06);
            border-radius: 12px;
            padding: 16px 18px;
            font-size: .88rem;
            color: var(--text);
            display: flex;
            align-items: center;
            gap: 10px
        }

        .award-card .award-icon {
            font-size: 1.2rem
        }

        .organizer-card {
            display: flex;
            gap: 20px;
            background: var(--surface2);
            border-radius: 16px;
            padding: 24px;
            border: 1px solid rgba(255, 255, 255, .06);
            align-items: flex-start
        }

        .organizer-card img {
            width: 72px;
            height: 72px;
            border-radius: 12px;
            object-fit: cover;
            flex-shrink: 0
        }

        .organizer-info h4 {
            font-family: var(--font-display);
            font-size: 1.2rem;
            margin-bottom: 6px
        }

        .organizer-info p {
            font-size: .82rem;
            color: var(--muted);
            line-height: 1.6;
            margin-bottom: 10px
        }

        .org-link {
            color: var(--gold);
            font-size: .85rem;
            display: inline-flex;
            align-items: center;
            gap: 4px;
            transition: gap .2s
        }

        .org-link:hover {
            gap: 8px
        }

        .modal-cta {
            width: 100%;
            padding: 16px;
            border-radius: 14px;
            font-size: .9rem;
            font-weight: 700;
            letter-spacing: .08em;
            text-transform: uppercase;
            margin-top: 8px
        }

        .modal-cta.sold-out {
            background: rgba(201, 76, 76, .2);
            color: var(--red);
            cursor: not-allowed;
            border: 1px solid rgba(201, 76, 76, .3)
        }

        /* COMING SOON */
        .coming-soon {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: calc(100vh - var(--nav-h) - 200px);
            text-align: center;
            padding: 80px 24px;
            position: relative;
            overflow: hidden
        }

        .cs-bg {
            position: absolute;
            inset: 0;
            pointer-events: none;
            opacity: .06
        }

        .cs-icon {
            font-size: 4.5rem;
            margin-bottom: 24px;
            animation: float 3s ease-in-out infinite
        }

        @keyframes float {

            0%,
            100% {
                transform: translateY(0)
            }

            50% {
                transform: translateY(-10px)
            }
        }

        .cs-eyebrow {
            font-size: .75rem;
            font-weight: 600;
            letter-spacing: .2em;
            text-transform: uppercase;
            color: var(--gold);
            margin-bottom: 16px
        }

        .cs-title {
            font-family: var(--font-display);
            font-size: clamp(2rem, 4vw, 3rem);
            font-weight: 400;
            margin-bottom: 16px;
            max-width: 500px
        }

        .cs-sub {
            color: var(--muted);
            font-size: .95rem;
            max-width: 460px;
            margin-bottom: 32px;
            line-height: 1.6
        }

        .cs-pill {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 8px 20px;
            border-radius: 100px;
            background: rgba(201, 168, 76, .1);
            border: 1px solid rgba(201, 168, 76, .2);
            color: var(--gold);
            font-size: .78rem;
            font-weight: 600;
            letter-spacing: .1em;
            margin-bottom: 36px
        }

        .cs-pill .pulse {
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background: var(--gold);
            animation: pulse 1.5s ease-in-out infinite
        }

        @keyframes pulse {

            0%,
            100% {
                opacity: 1;
                transform: scale(1)
            }

            50% {
                opacity: .4;
                transform: scale(.7)
            }
        }

        .cs-notify {
            display: flex;
            gap: 0;
            max-width: 400px;
            width: 100%
        }

        .cs-notify input {
            flex: 1;
            padding: 12px 18px;
            border-radius: 12px 0 0 12px;
            border: 1px solid rgba(255, 255, 255, .08);
            border-right: none;
            background: var(--surface2);
            color: var(--text);
            font-size: .88rem;
            font-family: var(--font-body);
            outline: none;
            transition: border-color .25s
        }

        .cs-notify input:focus {
            border-color: rgba(201, 168, 76, .4)
        }

        .cs-notify input::placeholder {
            color: var(--muted)
        }

        .cs-notify button {
            padding: 12px 24px;
            border-radius: 0 12px 12px 0;
            background: var(--gold);
            color: var(--bg);
            font-weight: 600;
            font-size: .85rem;
            transition: background .25s
        }

        .cs-notify button:hover {
            background: var(--gold-light)
        }

        /* FOOTER */
        footer {
            background: #080806;
            border-top: 1px solid rgba(201, 168, 76, .12);
            padding: 60px;
            display: grid;
            grid-template-columns: 1.2fr 1fr 1fr;
            gap: 48px
        }

        .footer-brand h3 {
            font-family: var(--font-display);
            font-size: 1.5rem;
            color: var(--gold);
            margin-bottom: 8px
        }

        .footer-brand .tagline {
            font-family: var(--font-display);
            font-style: italic;
            color: var(--muted);
            font-size: .95rem;
            margin-bottom: 12px
        }

        .footer-brand p {
            font-size: .82rem;
            color: var(--muted);
            line-height: 1.6;
            max-width: 320px
        }

        .footer-col h4 {
            font-size: .72rem;
            font-weight: 600;
            letter-spacing: .16em;
            text-transform: uppercase;
            color: var(--gold);
            margin-bottom: 18px
        }

        .footer-col a {
            display: block;
            font-size: .85rem;
            color: var(--muted);
            padding: 5px 0;
            transition: color .2s
        }

        .footer-col a:hover {
            color: var(--gold)
        }

        .social-row {
            display: flex;
            gap: 12px;
            margin-top: 16px
        }

        .social-icon {
            width: 36px;
            height: 36px;
            border-radius: 10px;
            background: var(--surface2);
            border: 1px solid rgba(255, 255, 255, .06);
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all .25s
        }

        .social-icon:hover {
            border-color: var(--gold);
            background: rgba(201, 168, 76, .1)
        }

        .social-icon svg {
            width: 16px;
            height: 16px;
            fill: var(--muted)
        }

        .social-icon:hover svg {
            fill: var(--gold)
        }

        .footer-bottom {
            grid-column: 1/-1;
            text-align: center;
            padding-top: 28px;
            border-top: 1px solid rgba(255, 255, 255, .04);
            font-size: .75rem;
            color: var(--muted)
        }

        .footer-bottom a {
            color: var(--muted);
            margin: 0 10px;
            transition: color .2s
        }

        .footer-bottom a:hover {
            color: var(--gold)
        }

        /* RESPONSIVE */
        @media(max-width:768px) {
            .nav-links {
                display: none
            }

            .hamburger {
                display: flex
            }

            .mobile-menu {
                display: flex
            }

            .events-hero {
                padding: 60px 24px 40px;
                min-height: auto
            }

            .filter-bar {
                padding: 12px 24px
            }

            .events-section {
                padding: 24px 20px 60px
            }

            .events-grid {
                grid-template-columns: 1fr
            }

            .modal-body {
                padding: 24px 20px 36px
            }

            .modal-hero {
                height: 220px
            }

            .modal-title {
                font-size: 1.5rem;
                left: 20px;
                right: 20px;
                bottom: 16px
            }

            .modal-chips {
                flex-direction: column
            }

            .awards-grid {
                grid-template-columns: 1fr
            }

            .impact-grid {
                flex-direction: column
            }

            footer {
                grid-template-columns: 1fr;
                padding: 40px 24px;
                gap: 32px
            }

            .organizer-card {
                flex-direction: column
            }

            .modal-overlay {
                padding: 20px 12px
            }
        }

        @media(max-width:480px) {
            .events-grid {
                grid-template-columns: 1fr
            }

            .card-img-wrap {
                height: 200px
            }
        }
    </style>
</head>

<body>

    <!-- GRAIN OVERLAY -->
    <div class="grain" aria-hidden="true">
        <svg>
            <filter id="grain">
                <feTurbulence type="fractalNoise" baseFrequency="0.65" numOctaves="3" stitchTiles="stitch" />
                <feColorMatrix type="saturate" values="0" />
            </filter>
            <rect width="100%" height="100%" filter="url(#grain)" />
        </svg>
    </div>

    <!-- NAVIGATION -->
    <nav class="nav" role="navigation" aria-label="Main navigation">
        <a href="#" class="nav-logo" onclick="showPage('home');return false">Estah Society</a>
        <div class="nav-links" id="navLinks">
            <a href="#" class="nav-link" data-page="home" onclick="showPage('home');return false">Home <span
                    class="soon-badge">SOON</span><span class="dot"></span></a>
            <a href="#" class="nav-link" data-page="about" onclick="showPage('about');return false">About Us <span
                    class="soon-badge">SOON</span><span class="dot"></span></a>
            <a href="#" class="nav-link active" data-page="events" onclick="showPage('events');return false">Events<span
                    class="dot"></span></a>
            <a href="#" class="nav-link" data-page="programs" onclick="showPage('programs');return false">Programs <span
                    class="soon-badge">SOON</span><span class="dot"></span></a>
            <a href="#" class="nav-link" data-page="impact" onclick="showPage('impact');return false">Impact <span
                    class="soon-badge">SOON</span><span class="dot"></span></a>
            <a href="#" class="nav-link" data-page="gallery" onclick="showPage('gallery');return false">Gallery <span
                    class="soon-badge">SOON</span><span class="dot"></span></a>
            <a href="#" class="nav-link" data-page="contact" onclick="showPage('contact');return false">Contact <span
                    class="soon-badge">SOON</span><span class="dot"></span></a>
        </div>
        <div class="hamburger" id="hamburger" onclick="toggleMobile()" aria-label="Toggle menu" role="button"
            tabindex="0"><span></span><span></span><span></span></div>
    </nav>

    <!-- MOBILE MENU -->
    <div class="mobile-overlay" id="mobileOverlay" onclick="toggleMobile()"></div>
    <div class="mobile-menu" id="mobileMenu" role="navigation" aria-label="Mobile navigation">
        <a href="#" class="nav-link active" data-page="events"
            onclick="showPage('events');toggleMobile();return false">Events</a>
        <a href="#" class="nav-link" data-page="home" onclick="showPage('home');toggleMobile();return false">Home <span
                class="soon-badge">SOON</span></a>
        <a href="#" class="nav-link" data-page="about" onclick="showPage('about');toggleMobile();return false">About Us
            <span class="soon-badge">SOON</span></a>
        <a href="#" class="nav-link" data-page="programs"
            onclick="showPage('programs');toggleMobile();return false">Programs <span class="soon-badge">SOON</span></a>
        <a href="#" class="nav-link" data-page="impact" onclick="showPage('impact');toggleMobile();return false">Impact
            <span class="soon-badge">SOON</span></a>
        <a href="#" class="nav-link" data-page="gallery"
            onclick="showPage('gallery');toggleMobile();return false">Gallery <span class="soon-badge">SOON</span></a>
        <a href="#" class="nav-link" data-page="contact"
            onclick="showPage('contact');toggleMobile();return false">Contact <span class="soon-badge">SOON</span></a>
    </div>

    <main>

        <!-- EVENTS PAGE -->
        <div class="page active" id="page-events">
            <section class="events-hero">
                <div class="hero-content">
                    <p class="eyebrow">Estah Society · Events 2026</p>
                    <h1 class="hero-title">Upcoming <em>Events</em></h1>
                    <p class="hero-sub">Join us in transforming communities through education, empowerment, and
                        sustainable action across India.</p>
                </div>
            </section>
            <div class="filter-bar" role="toolbar" aria-label="Event filters">
                <button class="filter-pill active" onclick="filterEvents(this)" aria-pressed="true">All Events</button>
                <button class="filter-pill" onclick="filterEvents(this)" aria-pressed="false">This Month</button>
                <button class="filter-pill" onclick="filterEvents(this)" aria-pressed="false">Sustainability</button>
                <button class="filter-pill" onclick="filterEvents(this)" aria-pressed="false">Education</button>
                <button class="filter-pill" onclick="filterEvents(this)" aria-pressed="false">Free Events</button>
            </div>
            <section class="events-section">
                <p class="section-label">All Upcoming Events · 1 Result</p>
                <div class="events-grid">
                    <article class="event-card" onclick="openModal()" tabindex="0" role="button"
                        aria-label="View Run for Education event details">
                        <div class="card-img-wrap">
                            <img src="https://skillbloomer.com/uploads/events/1770985327_698f176f3b880_featured.jpg"
                                alt="Run for Education event" loading="lazy"
                                onerror="this.style.display='none';this.nextElementSibling.style.display='flex'">
                            <div class="card-img-fallback" style="display:none">🏃</div>
                            <div class="card-img-overlay"></div>
                            <span class="card-date-badge">22 MAR</span>
                            <span class="card-cat-badge">#Sustainability</span>
                        </div>
                        <div class="card-body">
                            <h3 class="card-title">Run for Education</h3>
                            <div class="card-org">
                                <div class="card-org-avatar">ES</div><span>Estah Society</span>
                            </div>
                            <div class="card-meta">
                                <div class="card-meta-row"><svg width="14" height="14" viewBox="0 0 24 24" fill="none"
                                        stroke="#C9A84C" stroke-width="2">
                                        <rect x="3" y="4" width="18" height="18" rx="2" />
                                        <line x1="16" y1="2" x2="16" y2="6" />
                                        <line x1="8" y1="2" x2="8" y2="6" />
                                        <line x1="3" y1="10" x2="21" y2="10" />
                                    </svg><span>Sun, 22 March 2026 · 12:00 PM – 4:00 PM</span></div>
                                <div class="card-meta-row"><svg width="14" height="14" viewBox="0 0 24 24" fill="none"
                                        stroke="#C9A84C" stroke-width="2">
                                        <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z" />
                                        <circle cx="12" cy="10" r="3" />
                                    </svg><span>Gachibowli Stadium, Hyderabad, Telangana</span></div>
                            </div>
                            <p class="card-desc">An initiative by WE HEAL THE EARTH — run for a child's future, equal
                                opportunities, and a better tomorrow.</p>
                            <div class="card-divider"></div>
                            <div class="card-footer"><span class="sold-out-badge">SOLD OUT</span><span
                                    class="view-details">View Details →</span></div>
                        </div>
                    </article>
                </div>
            </section>
        </div>

        <!-- COMING SOON: HOME -->
        <div class="page" id="page-home">
            <div class="coming-soon">
                <div class="cs-bg"><svg width="100%" height="100%" viewBox="0 0 800 600">
                        <circle cx="400" cy="300" r="250" fill="none" stroke="rgba(201,168,76,.08)" stroke-width="1" />
                        <circle cx="400" cy="300" r="180" fill="none" stroke="rgba(201,168,76,.06)" stroke-width="1" />
                        <circle cx="400" cy="300" r="110" fill="none" stroke="rgba(201,168,76,.04)" stroke-width="1" />
                    </svg></div>
                <div class="cs-icon">🏠</div>
                <p class="cs-eyebrow">Home</p>
                <h2 class="cs-title">A New Home for Estah Society</h2>
                <p class="cs-sub">Our full homepage with our story, mission, and community highlights is in the works.
                </p>
                <div class="cs-pill"><span class="pulse"></span> COMING SOON</div>
                <div class="cs-notify"><input type="email" placeholder="Enter your email"
                        aria-label="Email for notification"><button type="button">Notify Me</button></div>
            </div>
        </div>

        <!-- COMING SOON: ABOUT -->
        <div class="page" id="page-about">
            <div class="coming-soon">
                <div class="cs-bg"><svg width="100%" height="100%" viewBox="0 0 800 600">
                        <polygon points="400,50 750,550 50,550" fill="none" stroke="rgba(78,140,111,.08)"
                            stroke-width="1" />
                        <polygon points="400,120 680,500 120,500" fill="none" stroke="rgba(78,140,111,.06)"
                            stroke-width="1" />
                        <polygon points="400,190 610,450 190,450" fill="none" stroke="rgba(78,140,111,.04)"
                            stroke-width="1" />
                    </svg></div>
                <div class="cs-icon">🌱</div>
                <p class="cs-eyebrow">About Us</p>
                <h2 class="cs-title">Our Story Unfolds Soon</h2>
                <p class="cs-sub">Learn about the people, purpose, and passion driving Estah Society's work across
                    India.</p>
                <div class="cs-pill"><span class="pulse"></span> COMING SOON</div>
                <div class="cs-notify"><input type="email" placeholder="Enter your email"
                        aria-label="Email for notification"><button type="button">Notify Me</button></div>
            </div>
        </div>

        <!-- COMING SOON: PROGRAMS -->
        <div class="page" id="page-programs">
            <div class="coming-soon">
                <div class="cs-bg"><svg width="100%" height="100%" viewBox="0 0 800 600">
                        <rect x="200" y="100" width="400" height="400" rx="40" fill="none" stroke="rgba(201,168,76,.06)"
                            stroke-width="1" transform="rotate(15 400 300)" />
                        <rect x="250" y="150" width="300" height="300" rx="30" fill="none" stroke="rgba(201,168,76,.04)"
                            stroke-width="1" transform="rotate(30 400 300)" />
                    </svg></div>
                <div class="cs-icon">📚</div>
                <p class="cs-eyebrow">Programs</p>
                <h2 class="cs-title">Explore Our Programs</h2>
                <p class="cs-sub">From scholarship exams to Education Fellowships — a full overview of how we create
                    impact is coming.</p>
                <div class="cs-pill"><span class="pulse"></span> COMING SOON</div>
                <div class="cs-notify"><input type="email" placeholder="Enter your email"
                        aria-label="Email for notification"><button type="button">Notify Me</button></div>
            </div>
        </div>

        <!-- COMING SOON: IMPACT -->
        <div class="page" id="page-impact">
            <div class="coming-soon">
                <div class="cs-bg"><svg width="100%" height="100%" viewBox="0 0 800 600">
                        <ellipse cx="400" cy="300" rx="300" ry="200" fill="none" stroke="rgba(78,140,111,.07)"
                            stroke-width="1" />
                        <ellipse cx="400" cy="300" rx="200" ry="130" fill="none" stroke="rgba(78,140,111,.05)"
                            stroke-width="1" />
                        <line x1="100" y1="300" x2="700" y2="300" stroke="rgba(78,140,111,.04)" stroke-width="1" />
                        <line x1="400" y1="100" x2="400" y2="500" stroke="rgba(78,140,111,.04)" stroke-width="1" />
                    </svg></div>
                <div class="cs-icon">🌍</div>
                <p class="cs-eyebrow">Impact</p>
                <h2 class="cs-title">Measuring What Matters</h2>
                <p class="cs-sub">Live data on students impacted, fellowships awarded, and communities transformed —
                    launching soon.</p>
                <div class="cs-pill"><span class="pulse"></span> COMING SOON</div>
                <div class="cs-notify"><input type="email" placeholder="Enter your email"
                        aria-label="Email for notification"><button type="button">Notify Me</button></div>
            </div>
        </div>

        <!-- COMING SOON: GALLERY -->
        <div class="page" id="page-gallery">
            <div class="coming-soon">
                <div class="cs-bg"><svg width="100%" height="100%" viewBox="0 0 800 600">
                        <rect x="100" y="100" width="200" height="150" rx="8" fill="none" stroke="rgba(201,168,76,.06)"
                            stroke-width="1" />
                        <rect x="350" y="200" width="200" height="150" rx="8" fill="none" stroke="rgba(201,168,76,.05)"
                            stroke-width="1" />
                        <rect x="500" y="80" width="180" height="120" rx="8" fill="none" stroke="rgba(201,168,76,.04)"
                            stroke-width="1" />
                        <rect x="150" y="320" width="220" height="160" rx="8" fill="none" stroke="rgba(201,168,76,.04)"
                            stroke-width="1" />
                    </svg></div>
                <div class="cs-icon">🖼️</div>
                <p class="cs-eyebrow">Gallery</p>
                <h2 class="cs-title">Memories of Impact</h2>
                <p class="cs-sub">Photos and moments from our events and programs across India — gallery coming soon.
                </p>
                <div class="cs-pill"><span class="pulse"></span> COMING SOON</div>
                <div class="cs-notify"><input type="email" placeholder="Enter your email"
                        aria-label="Email for notification"><button type="button">Notify Me</button></div>
            </div>
        </div>

        <!-- COMING SOON: CONTACT -->
        <div class="page" id="page-contact">
            <div class="coming-soon">
                <div class="cs-bg"><svg width="100%" height="100%" viewBox="0 0 800 600">
                        <path d="M200,300 Q400,100 600,300 Q400,500 200,300" fill="none" stroke="rgba(201,168,76,.06)"
                            stroke-width="1" />
                        <path d="M250,300 Q400,150 550,300 Q400,450 250,300" fill="none" stroke="rgba(201,168,76,.04)"
                            stroke-width="1" />
                    </svg></div>
                <div class="cs-icon">✉️</div>
                <p class="cs-eyebrow">Contact</p>
                <h2 class="cs-title">Let's Build Together</h2>
                <p class="cs-sub">Whether you're a partner, volunteer, donor, or simply curious — we'd love to connect.
                </p>
                <div class="cs-pill"><span class="pulse"></span> COMING SOON</div>
                <div class="cs-notify"><input type="email" placeholder="Enter your email"
                        aria-label="Email for notification"><button type="button">Notify Me</button></div>
            </div>
        </div>

    </main>

    <!-- EVENT DETAIL MODAL -->
    <div class="modal-overlay" id="modalOverlay" role="dialog" aria-modal="true" aria-label="Event details">
        <div class="modal" id="modalContent" role="document">
            <div class="modal-hero">
                <img src="https://skillbloomer.com/uploads/events/1770985327_698f176f3b880_featured.jpg"
                    alt="Run for Education"
                    onerror="this.style.display='none';this.parentElement.style.background='linear-gradient(135deg,#1A1A0D,#2A2000)'">
                <div class="modal-hero-overlay"></div>
                <button class="modal-close" onclick="closeModal()" aria-label="Close modal">✕</button>
                <h2 class="modal-title">Run for Education</h2>
            </div>
            <div class="modal-body">
                <div class="modal-chips">
                    <div class="modal-chip"><span class="chip-icon">📅</span>
                        <div>
                            <div class="chip-label">Date &amp; Time</div>
                            <div class="chip-value">March 22, 2026 · 12:00 PM – 4:00 PM</div>
                        </div>
                    </div>
                    <div class="modal-chip"><span class="chip-icon">📍</span>
                        <div>
                            <div class="chip-label">Venue</div>
                            <div class="chip-value">Gachibowli Stadium, Hyderabad</div>
                        </div>
                    </div>
                    <div class="modal-chip"><span class="chip-icon">🎫</span>
                        <div>
                            <div class="chip-label">Entry</div>
                            <div class="chip-value">Free · 0 Tickets Remaining</div>
                        </div>
                    </div>
                </div>

                <div class="modal-section">
                    <h3 class="modal-section-title">About This Event</h3>
                    <p>Run for Education is a heartfelt initiative by Estah Society to support children's learning and
                        spread awareness about the value of education. This event brings together students, parents,
                        teachers, and community members to stand for one common purpose — ensuring every child gets the
                        opportunity to learn, grow, and dream big.</p>
                    <p>Through this run, we encourage unity, hope, and social responsibility. The support received helps
                        us conduct scholarship exams, provide learning resources, and create educational opportunities
                        for deserving students who need encouragement the most.</p>
                    <p>Run for Education is not just about running; it is about running for a child's future, for equal
                        opportunities, and for a better tomorrow.</p>
                    <div class="mission-box">
                        <p>Our Mission: To inspire sustainable, solution-based educational practices that transform
                            communities and create lasting impact.</p>
                    </div>
                </div>

                <div class="modal-section">
                    <h3 class="modal-section-title">Impact Numbers</h3>
                    <div class="impact-grid">
                        <div class="impact-chip">
                            <div class="impact-num" data-target="70">0</div>
                            <div class="impact-label">Active Participants</div>
                        </div>
                        <div class="impact-chip">
                            <div class="impact-num" data-target="2500">0</div>
                            <div class="impact-label">Students Impacted</div>
                        </div>
                        <div class="impact-chip">
                            <div class="impact-num" data-target="25">0</div>
                            <div class="impact-label">Partner Institutions</div>
                        </div>
                        <div class="impact-chip">
                            <div class="impact-num" data-target="40000">0</div>
                            <div class="impact-label">Jobs Targeted</div>
                        </div>
                        <div class="impact-chip">
                            <div class="impact-num" data-target="4000000">0</div>
                            <div class="impact-label">Lives to Impact</div>
                        </div>
                    </div>
                </div>

                <div class="modal-section">
                    <h3 class="modal-section-title">2025 Award Categories</h3>
                    <div class="awards-grid">
                        <div class="award-card"><span class="award-icon">🏆</span> Education Innovator of the Year</div>
                        <div class="award-card"><span class="award-icon">⭐</span> Youth Changemaker Award</div>
                        <div class="award-card"><span class="award-icon">🌿</span> Sustainability Champion in Education
                        </div>
                        <div class="award-card"><span class="award-icon">🏛️</span> Sustainable Education Institution of
                            the Year</div>
                        <div class="award-card"><span class="award-icon">💼</span> Corporate Leadership in Education
                        </div>
                    </div>
                </div>

                <div class="modal-section">
                    <h3 class="modal-section-title">Organizer</h3>
                    <div class="organizer-card">
                        <img src="https://skillbloomer.com/uploads/store/uploads/organizers/RftXZ9cnL1Y5016F259HAtWHqJ76qhAU5w1eXltf.jpg"
                            alt="Estah Society" onerror="this.style.display='none'">
                        <div class="organizer-info">
                            <h4>Estah Society</h4>
                            <p>Estah Society is a community-driven non-profit empowering communities through education,
                                entrepreneurship, and sustainability under the WE HEAL THE EARTH framework. With a
                                vision for India 2047, Estah Society is committed to creating lasting impact through
                                innovative practices and community engagement.</p>
                            <a href="https://skillbloomer.com/events/run-for-education" target="_blank" rel="noopener"
                                class="org-link">View on SkillBloomer →</a>
                        </div>
                    </div>
                </div>

                <button class="modal-cta sold-out" disabled aria-label="Sold out, no tickets available">🚫 Sold Out — No
                    Tickets Available</button>
            </div>
        </div>
    </div>

    <!-- FOOTER -->
    <footer>
        <div class="footer-brand">
            <h3>Estah Society</h3>
            <p class="tagline">Run for a Child's Future</p>
            <p>Empowering communities through education, entrepreneurship, and sustainability under the WE HEAL THE
                EARTH framework.</p>
        </div>
        <div class="footer-col">
            <h4>Quick Links</h4>
            <a href="#" onclick="showPage('home');return false">Home</a>
            <a href="#" onclick="showPage('about');return false">About Us</a>
            <a href="#" onclick="showPage('events');return false">Events</a>
            <a href="#" onclick="showPage('programs');return false">Programs</a>
            <a href="#" onclick="showPage('impact');return false">Impact</a>
            <a href="#" onclick="showPage('gallery');return false">Gallery</a>
            <a href="#" onclick="showPage('contact');return false">Contact</a>
        </div>
        <div class="footer-col">
            <h4>Connect</h4>
            <p style="font-size:.82rem;color:var(--muted);margin-bottom:12px">Events powered by <a
                    href="https://skillbloomer.com" target="_blank" rel="noopener"
                    style="color:var(--gold)">SkillBloomer</a></p>
            <div class="social-row">
                <a href="#" class="social-icon" aria-label="Instagram"><svg viewBox="0 0 24 24">
                        <path
                            d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zM12 0C8.741 0 8.333.014 7.053.072 2.695.272.273 2.69.073 7.052.014 8.333 0 8.741 0 12c0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98C8.333 23.986 8.741 24 12 24c3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98C15.668.014 15.259 0 12 0zm0 5.838a6.162 6.162 0 100 12.324 6.162 6.162 0 000-12.324zM12 16a4 4 0 110-8 4 4 0 010 8zm6.406-11.845a1.44 1.44 0 100 2.881 1.44 1.44 0 000-2.881z" />
                    </svg></a>
                <a href="#" class="social-icon" aria-label="LinkedIn"><svg viewBox="0 0 24 24">
                        <path
                            d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 01-2.063-2.065 2.064 2.064 0 112.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z" />
                    </svg></a>
                <a href="#" class="social-icon" aria-label="WhatsApp"><svg viewBox="0 0 24 24">
                        <path
                            d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z" />
                    </svg></a>
                <a href="#" class="social-icon" aria-label="YouTube"><svg viewBox="0 0 24 24">
                        <path
                            d="M23.498 6.186a3.016 3.016 0 00-2.122-2.136C19.505 3.545 12 3.545 12 3.545s-7.505 0-9.377.505A3.017 3.017 0 00.502 6.186C0 8.07 0 12 0 12s0 3.93.502 5.814a3.016 3.016 0 002.122 2.136c1.871.505 9.376.505 9.376.505s7.505 0 9.377-.505a3.015 3.015 0 002.122-2.136C24 15.93 24 12 24 12s0-3.93-.502-5.814zM9.545 15.568V8.432L15.818 12l-6.273 3.568z" />
                    </svg></a>
            </div>
        </div>
        <div class="footer-bottom">
            <p>© 2026 Estah Society · WE HEAL THE EARTH Initiative</p>
            <a href="#">Privacy Policy</a><a href="#">Terms</a>
        </div>
    </footer>

    <script>
        // SPA ROUTING
        function showPage(name) {
            document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
            const page = document.getElementById('page-' + name);
            if (page) page.classList.add('active');
            // Update nav links
            document.querySelectorAll('.nav-link').forEach(l => {
                l.classList.toggle('active', l.dataset.page === name);
            });
            // Update mobile nav
            document.querySelectorAll('.mobile-menu .nav-link').forEach(l => {
                l.classList.toggle('active', l.dataset.page === name);
            });
            window.scrollTo({ top: 0, behavior: 'smooth' });
        }

        // FILTER
        function filterEvents(el) {
            document.querySelectorAll('.filter-pill').forEach(p => { p.classList.remove('active'); p.setAttribute('aria-pressed', 'false') });
            el.classList.add('active');
            el.setAttribute('aria-pressed', 'true');
        }

        // MOBILE MENU
        function toggleMobile() {
            const h = document.getElementById('hamburger');
            const m = document.getElementById('mobileMenu');
            const o = document.getElementById('mobileOverlay');
            const isOpen = m.classList.contains('open');
            h.classList.toggle('open');
            m.classList.toggle('open');
            if (isOpen) { o.classList.remove('open'); document.body.style.overflow = '' }
            else { o.classList.add('open'); document.body.style.overflow = 'hidden' }
        }

        // MODAL
        function openModal() {
            const ov = document.getElementById('modalOverlay');
            ov.classList.add('open');
            document.body.style.overflow = 'hidden';
            animateCountUp();
            // Focus trap
            ov.querySelector('.modal-close').focus();
        }
        function closeModal() {
            const ov = document.getElementById('modalOverlay');
            ov.classList.remove('open');
            document.body.style.overflow = '';
        }
        // Close on overlay click
        document.getElementById('modalOverlay').addEventListener('click', function (e) {
            if (e.target === this) closeModal();
        });
        // Close on ESC
        document.addEventListener('keydown', function (e) {
            if (e.key === 'Escape') closeModal();
        });
        // Enter key on card
        document.querySelector('.event-card').addEventListener('keydown', function (e) {
            if (e.key === 'Enter') openModal();
        });

        // COUNT UP ANIMATION
        function animateCountUp() {
            document.querySelectorAll('.impact-num').forEach(el => {
                const target = parseInt(el.dataset.target);
                const duration = 1500;
                const start = performance.now();
                el.textContent = '0';
                function update(now) {
                    const elapsed = now - start;
                    const progress = Math.min(elapsed / duration, 1);
                    const eased = 1 - Math.pow(1 - progress, 3);
                    let val = Math.floor(eased * target);
                    if (target >= 1000000) el.textContent = (val / 1000000).toFixed(1) + 'M+';
                    else if (target >= 10000) el.textContent = Math.floor(val / 1000).toLocaleString() + 'K+';
                    else el.textContent = val.toLocaleString() + '+';
                    if (progress < 1) requestAnimationFrame(update);
                }
                requestAnimationFrame(update);
            });
        }
    </script>

</body>

</html>